# Trek ThumbDrive udev rule
# Allows non-root access to the device for users in the "plugdev" group.
#
# Install:
#   sudo cp 50-trek.rules /etc/udev/rules.d/
#   sudo udevadm control --reload-rules
#   sudo udevadm trigger
#
# Then re-plug the device.

SUBSYSTEMS=="usb", ATTRS{idVendor}=="0a16", ATTRS{idProduct}=="1111", MODE="0666", GROUP="plugdev"
